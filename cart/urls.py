# from django.urls import path
# from .views import AddToCartView,CartHomeView

# app_name = 'cart'

# urlpatterns = [
#     path('cart/', CartHomeView.as_view(), name='cart_home'),
# # urls.py
# path('add-to-cart/<int:barcode>/', AddToCartView.as_view(), name='add_to_cart'),
# ]
